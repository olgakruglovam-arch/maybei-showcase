# Majlis hero polish and waitlist QA

The MAJLIS COVER helper label has been removed. The AI-head cover retains its wide framing and uses a pointer-only parallax offset, while the reduced-motion media query disables both animation and transforms. The hero CTA now targets the waitlist section. Desktop and 375px mobile previews show a readable form layout without horizontal overflow; the Playwright flow covers the hero jump and confirmation state, and API unit tests cover validation, created and duplicate outcomes.
